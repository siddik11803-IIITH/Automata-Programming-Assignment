#include<bits/stdc++.h>
using namespace std;


int main(){
    int n;
    cin >>  n ;
    char states[n];
    int i_1[n];
    int i_2[n];
    int i_3[n];
    for(int i = 0; i < n; i++){
        cin >> states[i] >> i_1[i] >> i_2[i]>> i_3[i];
    }
    // Now we have stored the variables and stuff in the arrays
    
    
    return 0;
}